{
  "scripts": {
    "start": "npx esbuild --bundle --format=iife src/app.ts --outfile=docs/js/app.js --servedir=docs --watch",
    "start:ce": "npx serve docs",
    "build": "npx esbuild --bundle --format=iife src/app.ts --outfile=docs/js/app.js --minify"
  },
  "dependencies": {
    "serve": "^14.2.1",
    "vanjs-core": "^1.2.8",
    "vanjs-ext": "^0.2.0"
  },
  "devDependencies": {
    "esbuild": "^0.19.12"
  }
}
