{
  "name": "van-timer",
  "version": "1.0.0",
  "description": "Timer implementation using VanJS",
  "main": "index.js",
  "scripts": {
    "start": "vite",
    "build": "tsc -b && vite build --emptyOutDir --outDir ../../dist/van --base /van/",
    "build:main": "tsc -b && vite build --outDir ../../dist/ --base /"
  },
  "keywords": [
    "vanjs"
  ],
  "author": "Jon Nyman",
  "license": "ISC",
  "devDependencies": {
    "typescript": "^5.8.3",
    "vite": "^6.3.5",
    "vite-plugin-compression2": "^2.2.0"
  }
}
